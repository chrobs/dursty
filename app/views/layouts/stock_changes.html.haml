- content_for :stockChangesMenu do
  = render "stock_changes/submenu"

- content_for :left do
  = render "shared/menu"
  - if user_signed_in?
    = render "home/user"

- content_for :content do

  = content_for?(:home_content) ? yield(:home_content) : yield

- content_for :pathway do
  &raquo; #{link_to "Lager", stock_index_path} #{yield(:pathway_sub)}

= render :template => 'layouts/application'

- # fix max width for main-element
:javascript
  $("#main").attr("style","max-width:none")
