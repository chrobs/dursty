= content_for :pathway_sub do
  &raquo; #{@user.name}

= semantic_form_for @user do |f|
  = f.input :name
  = f.input :email
  %h2 Verkaufsorte
  = f.input :oph, :as => :boolean
  = f.input :tvk, :as => :boolean
  %h2 Spezielle Ämter
  = f.input :kassenwart, :as => :boolean
  = f.input :lagerwart, :as => :boolean
  = f.input :admin, :as => :boolean
  = f.actions
