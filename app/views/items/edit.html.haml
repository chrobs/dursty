- content_for :pathway_sub do
  &raquo; Editiere "#{@item.name}"

= semantic_form_for @item do |f|
  = f.input :name
  = f.input :preis
  = f.actions do
    = f.action :submit, :as => :button, :label => "zurück"
    = f.action :submit, :as => :button, :label => "speichern"
